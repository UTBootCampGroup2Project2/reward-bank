<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      {{comment_text}}
    </div>
    <br>
    <div class="meta">
      &#x2014; {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>